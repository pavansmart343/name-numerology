# Canonical redirect: trailing-slash → no trailing slash (301)
# Cloudflare Pages natively handles trailing slash normalization based on output format.
# We leave this file empty to avoid redirect loops and chains.
